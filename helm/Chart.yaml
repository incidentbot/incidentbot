apiVersion: v2
name: incident-bot
description: >-
  Umbrella chart deploying the BenefexLtd fork of incident-bot to the ops
  cluster (onehub-ew3-ops-p). Wraps the upstream incidentbot chart and adds an
  ExternalSecret and a Prometheus ServiceMonitor.
type: application
version: 0.1.0
appVersion: "3.0.3"
dependencies:
  - name: incidentbot
    version: 3.0.3
    repository: https://docs.incidentbot.io/charts
